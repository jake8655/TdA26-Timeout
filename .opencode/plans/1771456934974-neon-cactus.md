# Plan: Course dashboard refactor + lifecycle fixes

## Scope
- Frontend: split `apps/web/src/app/dashboard/courses/[uuid]/page.tsx`, adopt date-fns-tz, schedule defaults/constraints, status actions from `courseStatusActions`, duplicate & delete redirect, quiz stats empty state, student course filtering
- Backend: ensure pause triggers kick

## Frontend changes
- Create focused components under `apps/web/src/components/courses/`
  - `course-header.tsx` (title, status, metadata)
  - `course-actions.tsx` (buttons + dialogs)
  - `course-schedule-dialog.tsx` (schedule/start dialogs)
  - `course-materials-section.tsx` (list + cards)
  - `course-quizzes-section.tsx` (list + cards)
- Add schedule/date helpers under `apps/web/src/lib/course-date-utils.ts`
  - Use `date-fns` + `date-fns-tz` for conversions/formatting
  - Replace `toUtcIso`, `mergeDateTime`, `formatCourseTime`, `toLocalInput`, and timezone helpers
- Update schedule defaults and constraints
  - Default start = now + 5 minutes
  - Default end = start + 1 hour
  - On start change, if end < start then set end = start + 1 hour
- Wire action visibility to `courseStatusActions` from `apps/web/src/lib/utils.ts`
- Duplicate course: on success, navigate to new course UUID
- Delete course: on success, navigate to dashboard
- Quiz stats dialog: treat 403 (course not live) as empty state instead of error
- Student list `/courses`: filter out draft/archived even if server sends them; handle `joined` missing safely
- Add `date-fns-tz` dependency in `apps/web/package.json`

## Backend changes
- Centralize kick mechanism for pause/end
  - Add helper in `CourseLifecycleService` to deactivate joins + broadcast `course_kick`
  - Use helper in `CourseLifecycleController` for PAUSED + ARCHIVED (status update and pause endpoint)
  - Reuse helper in scheduled end path

## Verification
- Web UI manual checks
  - `/dashboard/courses/[uuid]`: actions shown per status map, schedule defaults correct, end auto-adjusts
  - Duplicate -> redirects to new course detail
  - Delete -> redirects to dashboard
  - Quiz stats on non-live course shows empty state, no error toast
  - `/courses` hides draft/archived; view/continue updates after join
- Server behavior
  - Pause triggers kick broadcast
- Optional commands (if you want)
  - `bun check` and `bun types` in `apps/web`
  - `./gradlew build` in `apps/server`
